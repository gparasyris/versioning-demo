app:
  build: .
  cached: true
  volumes:
    - .:/usr/src/app
    - /usr/src/app/node_modules
semantic_release:
  build:
    image: semantic_release
    dockerfile: Dockerfile-semantic-release
  encrypted_env_file:
    - github.token.encrypted
